\appendices
\section{Supplementary Diagnostics}

\subsection{Regression details}
\label{app:regression}
Table~\ref{tab:regression} reports both models over $n=\regN$ points per engine. Traversal
counts are engine-specific, regressor correlation reaches \regCorr, and wide intervals
plus leave-one-out error make coefficient-level attribution unsupported.
\input{tables/regression}

\subsection{EVT diagnostics}
\label{app:evt}
\input{tables/evt}
Table~\ref{tab:evt} retains the fitted tails only to document rejection: serial dependence
invalidates their quantiles and intervals. A pooled Gumbel estimate only
\evtGumbelExtraPct\% above the observed maximum illustrates why fit appearance is insufficient.

\subsection{Cross-ISA discrepancy diagnosis}
\label{app:crossisa-debug}
The initial AArch64 mismatch on \emph{search-01} reproduced under QEMU and hardware.
Bisection found a stored expectation predating the pcl f32 fidelity fix, not an ISA effect;
re-baselining made host, emulator, and hardware counters bit-identical.
