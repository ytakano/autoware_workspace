% HAND-MAINTAINED (structural facts, not measurements) -- not touched by gen_tables.py.
\begin{table}[t]
\centering
\caption{Preconditions of the conditional Rust work envelope~\eqref{eq:rust-envelope}: each limit, its enforcer, and the
behavior on violation. ``assumed'' rows are not runtime-checked by the engine.}
\label{tab:preconditions}
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{@{}lllp{0.22\columnwidth}@{}}
\toprule
quantity & limit & enforced by & on violation \\
\midrule
$P$ & \runtimePMax & engine limit & return source-limit error \\
$N_{\mathrm{iter}}$ & 30 & engine and loop guards & reject larger setting \\
line-search passes & 0 extra & shipped flag off & outside proposition if enabled \\
$K$ (Rust) & 64 & hard search cap & warn; force non-converged \\
$K$ (C++) & none & --- & grows with tile overlap \\
$K$ (production input) & 27 & map pipeline (assumed) & production-input bound invalid \\
$N_{\mathrm{leaves}}$ & \runtimeLeavesMax & map finalization & reject; keep old map \\
kd query stack & 64 entries & build-depth guard & return map-build error \\
serial scratch & \runtimeScratchBytes{} B & startup reservation & reject undersized scratch \\
integer products & counter range & checked construction & return arithmetic error \\
numeric inputs & finite & align entry checks & return non-finite error \\
\bottomrule
\end{tabular}
\end{table}
